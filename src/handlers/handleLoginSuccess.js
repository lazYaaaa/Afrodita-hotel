const handleLoginSuccess = (userData, setUser) => {
    
    setUser(userData); // Вызываем setUser с данными пользователя
    // Здесь можно добавить другую логику, если нужно
};
export default handleLoginSuccess;